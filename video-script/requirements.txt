# 桌面 GUI
PySide6>=6.6.0

# AI API
openai>=1.50.0        # 智谱 + DeepSeek 共用（均兼容 OpenAI SDK）
anthropic>=0.30.0     # Claude（可选，保留兼容）

# 视频/音频处理
opencv-python>=4.8.0
faster-whisper>=1.0.0
pyannote.audio>=3.1.0
scenedetect[opencv]>=0.6.3

# 工具
python-dotenv>=1.0.0
Pillow>=10.0.0
numpy>=1.24.0

# 导出
fpdf2>=2.7.0          # PDF 导出
markdown>=3.5.0       # Markdown 处理
